import React from "react"; export function Badge({children,variant="default",className=""}:{children:React.ReactNode;variant?:"default"|"outline"|"secondary";className?:string;}){ const styles=variant==="outline"?"border border-neutral-600 text-neutral-200":variant==="secondary"?"bg-neutral-800 text-neutral-100":"bg-neutral-200 text-neutral-900"; return <span className={`inline-flex items-center px-2 py-0.5 rounded-full text-xs ${styles} ${className}`}>{children}</span>} export default Badge;
